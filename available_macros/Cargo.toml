[package]
name = "available_macros"
version = "0.1.0"
edition = "2021"

[lib]
proc_macro = true

[dependencies]
syn = {version="2.0", features=["full", "extra-traits", "visit-mut"]}
quote = "1.0"
lazy_static = "*"
proc-macro2="*"
